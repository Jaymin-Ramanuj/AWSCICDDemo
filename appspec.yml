version: 0.0
os: linux

files:
  - source: /
    destination: /var/www/awscicddemo

hooks:
  # To deploy and run docker image on EC2
  AfterInstall:
    - location: scripts/deploy_docker.sh
      timeout: 300
      runas: ec2-user

# Commented code for normal deployment on EC2
#  BeforeInstall:
#    - location: scripts/before_install.sh
#      runas: root

#  AfterInstall:
#    - location: scripts/after_install.sh
#      runas: root

#  ApplicationStart:
#    - location: scripts/start_app.sh
#      runas: root
